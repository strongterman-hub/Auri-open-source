# 版本与验证记录

## 2026-09-16：聊天背景清晰度、角色头像联动与设置文案（Android 0.3.33）

- 服务端 `GET /v1/persona/me` 的 `characters[]` 新增 `avatar_url`，可从 `server/app/static/portrait/avatar/{presentation}.jpg` 提供角色头像；公开仓库包含两张渐变占位头像，真实头像只在私有部署。
- Android 0.3.33 / versionCode 36：动态背景在人物中下部使用更轻的四段 Aurora 遮罩；智能背景开启时助手气泡 78% 不透明度并加细描边，时间戳加深色底板，用户气泡保持不透明；聊天设置头像按当前「性别」选项加载，可见文案改为「性别 / 性格」。
- 公开服务端完整回归 **388 passed**；唯一失败为既有 `test_understanding.py::test_deepseek_structured_toggle_and_finish_diagnostics` 的本机 httpx 代理端口解析环境问题。源码检查 0 问题；公开 Android `testStoreDebugUnitTest`、`assembleStoreDebug`、`assembleDirectDebug` 通过。
- 公开仓库不包含官方 APK、签名、真实角色成图、生产配置、账号或部署数据。

## 2026-09-15：人设形象与动态背景（Android 0.3.31）

- 新增 female / male 形象维度与 `CharacterCard`，与现有四个性格预设正交；聊天与主动提示词会附加少量口癖、兴趣和 emoji 偏移。
- 服务端新增确定性背景解析：按用户时区、睡前睡眠分、30 分钟内运动、6 小时内情绪信号和关系阶段，从 12 个变体中选图；`portrait_state` / `portrait_settings` 表与 `user_persona.presentation` 列均幂等迁移。
- 新增 `GET /v1/portrait/current`、`GET/PUT /v1/portrait/settings`；关闭或未灰度账号返回 `enabled=false` 与安全兜底，不写状态。
- Android 0.3.31 引入 Coil 2.6.0，聊天页背景 400ms 交叉淡入并保留 Aurora 渐变遮罩；账号中心新增「智能背景」开关，接口失败静默回退渐变。
- 公开服务端完整回归 387 passed，另 1 项 `test_understanding` 为本机既有 httpx 代理端口解析环境失败；portrait / persona 专项 22 passed。公开 Android `assembleStoreDebug` 与 19 项 JVM 单测通过。
- 公开仓库只包含程序生成的渐变占位图（48 张，约 0.44 MB），不包含 Qwen 生成的真实角色成图；默认 `AURI_PORTRAIT_ENABLED=false`，启用后建议先用 `AURI_PORTRAIT_CANARY_USER_IDS` 灰度。

## 2026-09-15：可配置人设与关系行为第一版

- 新增 `app/persona/`：可配置 JSON 人设预设、关系状态、open loops、逐轮行为策略和提示词块；内置 `warm_friend`、`playful`、`calm`、`efficient` 四个预设，部署可覆盖。
- 聊天侧按用户输入长度锚定回复篇幅，低信息消息可选择沉默或延后，未结问题阻止重复提问；fast 只保留问题、任务、安全、账号等场景。
- 主动消息保持高频优先：默认 `high` 档位、日上限 24 条，通过健康、天气、explore、casual_checkin、self_share、memory_recall 等品类配额控制内容配比，而不是简单降频。
- 新增 `GET /v1/persona/presets`、`GET/PUT /v1/persona/me`；用户选择默认关闭，后续可由客户端接入，不需要服务端发版。`memory.db` 幂等新增关系状态、用户人设、open loops 和 agent notes 表。
- 公开服务端定向套件 **105 passed**；本机完整回归 **338 passed**，另有 1 项既有 httpx monkeypatch 环境和 35 项 pytest `tmp_path` 权限错误，与代码无关。源码检查覆盖 358 个跟踪文件、0 问题。本轮为服务端更新，Android 无需升级。
- 高频主动行为是否适合每个部署仍取决于用户偏好和内容质量；默认值可配置，canary、用户选择 API 和服务端运行日志可用于逐步验证。

## 2026-09-14：记忆理解与纠错闭环第一版

- 事件抽取增加持久化任务、纠错优先、租约恢复及有限重试；辅助结构化调用区分截断与无事件，旧任务不能覆盖更新的事件状态。
- 普通聊天与主动消息共用带时间和来源的事实约束；事件召回结合主题、纠错、近期更新及有效的进行中状态。
- 睡眠日累计、主睡眠区间、明确午睡和运动发生时间分别解释；健康分享纳入连续性检查，近期未答问题独立保留。
- 事实聊天草稿在发送前校验，必要时使用允许的只读工具；纠错不计为积极互动。日程提醒只在明确关联的进度证据充分时跳过，检查不可用时保留提醒。

公开服务端全量 **349 passed**，源码扫描 342 个跟踪文件、0 问题；存在一项既有 pytest `asyncio_mode` 配置警告。本轮为服务端更新，Android 无需升级。第一版仍采用词项匹配与明确纠错模式，额外校验会增加部分回复的耗时和 token 用量；自动化回归不代表所有自然对话都已正确理解。详见 [实现与边界](MEMORY_UNDERSTANDING.md)。

## 2026-09-13：主动消息睡眠门控可靠解锁

- 个人睡眠 episode 不再把睡眠进行中滚动更新的临时 `session_end` 直接视为醒来；结束点必须接近个人预计醒来时间，并在输入稳定达到配置阈值后才可触发提前放行。
- 硬截止在固定醒来尾量之外加入有上限的个人醒来离散度，同时继续受最大睡眠窗口约束。即使没有最终睡眠记录、分期或用户活动，也会自动解锁，不会永久依赖用户先发消息。
- 明确用户活动和新鲜清醒分期仍可提前解锁，新鲜睡眠分期仍优先阻断；睡眠结束放行增加不含消息正文的 `sleep_release` 审计。
- 新增临时尾点、输入稳定、醒来离散度和硬截止回归，公开服务端专项 77 项、全量 **317 passed**，`compileall` 通过；存在一项既有 pytest `asyncio_mode` 配置警告。

本次只修改服务端和公开说明，不修改 Android、不发布 APK。公开仓库不包含生产账号、睡眠记录、日志或线上数据库；自动化验证也不等同于真实设备推送送达。

## 2026-09-12：主动消息短期连续记忆与防失忆

- 主动情境从固定末 8 条消息改为 4 条即时原文，并独立保留 72 小时内最近 12 条用户中心问答；助手主动分享不再挤掉用户刚给出的答案。
- 事件抽取读取当前消息前 4 条有限对话，用于理解省略式回答；新增 `event_memory.jsonl` 隐私最小化结果审计，不记录正文或模型原始输出。
- 问题、软问候、记忆回访、探索和目标提醒在发送前检查近期问答、事件及长期记忆，返回 `safe / rewrite / block`。已有答案时延续改写或静默，检查失败时高风险草稿安全静默。
- “刚说过、你失忆了、怎么又问”等明确纠错不再计为积极回复，并触发有限互动提问暂停；decision/pacing SQLite 使用幂等加列迁移。

公开服务端专项 79 项、全量 **300 passed**，`compileall` 通过，源码检查覆盖 334 个跟踪文件且为 0 问题；存在一项既有 pytest `asyncio_mode` 配置警告。本次是服务端源码变更，不修改 Android、不发布 APK；生产账号、日志、数据库与线上验收数据不属于公开仓库。

## 2026-09-12：Android 0.3.30 多品牌后台收消息优化

- 官网直装包的后台连接不再依赖“主动消息”开关；在已登录、同意隐私且系统允许通知时用于聊天回复、主动消息和日程提醒，退出登录、撤回同意或关闭通知后停止。
- 通知状态同时检查 Android 13 运行时权限、应用通知总开关和 `auri_messages` 渠道；JPush 从 5.8.0 升级到 6.2.1，并显式允许后台长连接。
- 设备 registration ID 在变化时立即补登记，并每 24 小时幂等刷新一次，以便服务端登记异常丢失后自恢复。
- 小米、OPPO/一加/realme、vivo、华为、荣耀、三星和魅族使用各自的后台设置名称及候选入口；未知品牌不再统一显示虚假的“自启动权限”。候选入口不可用时回退应用详情。
- 服务端通知统一指定 `auri_messages`，离线保存时间为 6 小时，避免后台连接恢复后补达过旧内容。

私有/公开服务端完整回归分别为 307/293 项通过，公开源码扫描 330 个跟踪文件、0 问题；私有和公开 Android 的 Direct/Store JVM 测试及两渠道 Debug 均通过，私有签名 Direct Release 和 lintVital 通过。正式 APK 的构建与生产发布不属于公开源码产物；没有物理真机和 OEM 私有设置页验收，不能据此承诺应用被强制停止后仍必达。本版不包含消息送达检测或厂商系统推送通道。

## 2026-09-11：DeepSeek V4.1 Flash 计费更新

- 按 DeepSeek 2026-09-10 公告更新 Flash 费率：高峰缓存命中 / 未命中 / 输出为 `0.04 / 2 / 8` 元每百万 tokens，空闲时段为 `0.02 / 1 / 4`；高峰仅限北京时间工作日 `9:00-12:00`、`14:00-18:00`。
- `deepseek-flash`、`deepseek-v4-flash`、`deepseek-v4-flash-vision-exp` 统一按 V4.1 Flash 计价；`deepseek-v4-pro` 在北京时间 2026-09-14 12:00 起自动切换为相同费率。
- 计价按官方生效时间版本化，后台重算生效前日志时继续使用原 Auri 费率，避免历史费用展示漂移。Credits 兑换与扣费换算规则未变。

公开服务端完整回归 293 项通过，存在一项既有 pytest `asyncio_mode` 配置警告。官方费率来源见 [价格说明](https://api-docs.deepseek.com/zh-cn/quick_start/pricing) 与 [V4.1 Flash 公告](https://api-docs.deepseek.com/zh-cn/news/news260910)。生产账户、账本和历史补差不属于公开源码。

## 2026-09-09：Android 0.3.29 与账号、网络、隐私修复

- 注销接口从 Authorization 请求头读取 Bearer 凭证并撤销当前令牌；缺失、错误、过期凭证返回 401。其他设备令牌不受影响。客户端等待服务端结果，失败时保留登录状态并提示。
- 隐私勾选同时保存同意和不同意，记录政策版本及变更时间；升级后旧政策记录需要重新确认，撤回时停止推送并更新 SDK 授权状态。
- 用户消息展示“发送中”，断网立即显示可重试失败状态；连接和请求超时映射为中文提示。重启后未完成消息可重试，并继续使用原消息 ID 防止重复。
- 登录页、升级确认及账号中心提供完整离线隐私政策；官网新增 `/privacy` 和页脚入口，涵盖账号、对话、附件、健康、位置、第三方服务、存储及联系渠道。

公开服务端本地完整回归 288 项通过。官方服务端回归 302 项通过。官方签名 Direct 包在 Android 15 隔离模拟器上完成旧版升级、隐私确认、断网与恢复重试、注销流程检查；未完成物理真机验收，不代表任何商店审核通过。首次引导消息节奏不在本轮修改范围。

## 2026-09-08：好友式回复长度优化

异步回复规划从单一的“是否回复、何时回复”扩展为同时选择 `micro / short / normal / detailed` 篇幅档位。普通聊天默认采用最小充分回应，减少复述、报告式展开、机械总结和固定追问；明确详细任务、附件以及健康/安全完整性需要时仍可升档。篇幅随持久回复任务保存，旧 SQLite 数据库启动时幂等增加 `verbosity` 列，不重建历史任务。

主动消息同步限制为一次只表达一个观察、念头或问题。轮次和异步回复审计新增档位元数据，但不记录消息正文。本地公开版服务端完整回归 **282 passed**，源码检查覆盖 321 个跟踪文件且为 0 问题；生产部署、真实账号数据、模型费用和回滚路径不属于公开源码证据。

## 2026-09-07：后台运营中心

管理后台从单一长页重构为响应式运营中心，分为总览、用户与 Credits、对话与工具、版本发布、系统与费率。新增周期与完整账号筛选、用量和轮次服务端分页、模型/类型/状态/工具/关键字筛选、北京时间趋势、错误与慢轮次提示、工具耗时详情、Credits 只读汇总，以及配置、调度器和日志状态。版本操作继续分成 APK 上传、元数据保存和更新检查推送三步，均需显式确认。

后台和登录页复用仓库已有的正式 Auri App icon，浏览器标签页同步使用该图标；没有增加第三方图片、字体或 CDN。账号标识只在管理员认证后完整显示，overview/users 接口不返回密钥、聊天正文或位置。Credits 汇总只读，不创建账户或发放欢迎 Credits。

本地后台相关 19 项测试通过，服务端完整回归 289 项通过，存在一项既有 pytest `asyncio_mode` 配置警告。生产部署、官方账号数量、费用、调用量及回滚路径不属于公开源码证据；自部署者应使用自己的数据和凭据单独验收。

## 2026-09-06：精简日程选择入口与 Android 0.3.28

移除日程编辑页开始日期、开始时间、结束日期、结束时间及重复截止日期字段右侧重复出现的“选择”文字。字段名称和当前值保持左对齐，整个描边区域仍可点击，日期月历与小时/分钟滚轮的交互不变。

本地 Direct、Store 两渠道各 10 项 JVM 测试通过，两渠道 Debug、官方签名 Direct Release 与 lintVital 构建通过。隔离模拟器实际打开新建日程页，UI 层级确认不存在独立的“选择”文本，四个日期/时间字段均正常显示，点按开始日期仍能打开月历。

## 2026-09-06：日程日期和时间选择器与 Android 0.3.27

日程编辑页不再要求手动输入 `YYYY-MM-DD` 或 `HH:mm`：开始、结束和重复截止日期使用日历选择器；开始与结束时间使用可独立滑动的小时、分钟双列滚轮，并在停止滑动时吸附到具体数值。开始日期移到结束日期之后时，结束日期会自动跟随，避免产生倒置日期。

本地验证覆盖日期选择与回填、小时和分钟真实滑动、开始/结束时间回填，以及保存后服务端系列数据精确保留选择值。Direct、Store 两个渠道各 10 项 JVM 测试通过，两渠道 Debug 和官方签名 Direct Release 构建通过。正式直装版已发布；官方 APK、签名和生产配置不纳入源码仓库。

## 2026-09-06：共享日程与 Android 0.3.26

本次源码同步对应 Android **0.3.26 / versionCode 29**。新增 Auri 与用户共用的日程：Android 侧边栏提供月历和每日安排，支持手动新增、编辑、完成和取消；聊天工具可查询空档及新建、修改、取消日程；服务端统一处理全天、跨天、每天/工作日/每周重复、单次例外、版本冲突和提醒去重。主动情境可以读取近期日程，但只把它视为计划，不会据此断言用户正在做某件事。

官网同步增加日程介绍。官方直装版已通过版本化下载地址发布；开源仓库仍只提供源码，不包含官方 APK、签名、生产配置或用户数据。

本次本地验证结果：

| 验证项 | 结果 |
|---|---|
| 服务端完整测试集 | 271 passed；存在一项既有 pytest-asyncio 配置警告 |
| Android `testStoreDebugUnitTest` | 9 tests，0 failures |
| Android `assembleStoreDebug` | 通过 |
| Android `assembleDirectDebug` | 通过 |
| 官方 Direct Release | 已签名构建并通过版本、v2 签名和 SHA-256 校验；APK 不纳入源码仓库 |

Android 构建仍有既有 Kotlin API 和 Gradle 弃用提示，未阻塞测试或构建。本轮源码验证没有进行开源实例的设备 UI 操作、真实模型对话、系统/第三方日历同步或物理真机安装。

## 2026-09-05：首次开源快照

日期：2026-09-05。Android 基线为 **0.3.25 / versionCode 28**。

本说明记录首次开源快照的本地准备结果。源码已于 2026-09-05 发布到 [GitHub 公开仓库](https://github.com/strongterman-hub/Auri-open-source)，首次源码提交为 `c27af81`。本次发布没有产生新的官方 APK，也没有部署生产服务。

推送会触发服务端测试和 Android 构建，最新远程结果见 [GitHub Actions](https://github.com/strongterman-hub/Auri-open-source/actions/workflows/ci.yml)。下表记录本地验证；远程状态以对应提交的运行记录为准。

## 源码范围

服务端与 Android 放在同一个仓库的 `server/`、`android/` 中，保留当前完整业务实现：聊天与异步回复、记忆、主动消息、工具、健康、账号、Credits、支付宝、管理后台、官网及直装更新。

这是从当前工作区整理的新快照，未携带旧 Git 历史。官方运行数据、配置凭据、签名文件、私人运维脚本、缓存和构建产物未纳入发行内容。网站仅保留实际使用、已经公开展示的资源。

服务端与根目录原创文档/工具采用 AGPL-3.0-only，Android 原创代码采用 MIT；第三方来源及许可证见 [第三方声明](../THIRD_PARTY_NOTICES.md)。

## 为自部署作出的调整

- 提供不需要模型 Key 的 Echo 示例配置，默认关闭邮件验证、扣费、支付、主动调度和云健康同步；真实能力按配置启用。
- Android 默认使用社区包名，Debug 连接模拟器宿主机，Release 地址由实例维护者配置。
- 推送 Key 改为可选构建参数；没有 Key 时跳过 JPush 初始化。正式签名由维护者自行提供，没有签名配置也可构建未签名 Release。
- 补充服务端 Python 包发现及静态资源配置，支持按文档进行 editable 安装。
- 全新 Windows 环境发现缺少 IANA 时区数据库会导致启动失败，已显式加入 `tzdata` 依赖。
- 补充中英文介绍、产品与架构、配置默认值、两端开发、部署、贡献、安全反馈和持续集成配置。
- 开源快照的官网页尾增加源码入口；部署修改版时，运营者需要把该入口指向实际运行版本的对应源码。

## 本地验证结果

| 验证项 | 结果 |
|---|---|
| 全新 Python 3.12 虚拟环境安装 `server[dev]` | 通过 |
| 服务端完整测试集 | 255 passed；存在上游测试工具的两项弃用警告 |
| 本地 HTTP Smoke | 健康检查、随机测试账号注册、会话、同步回复、异步回复、幂等重试通过 |
| Android `testStoreDebugUnitTest` | 7 tests，0 failures |
| Android `assembleStoreDebug` | 通过 |
| Android `assembleDirectDebug` | 通过 |
| Android `assembleStoreRelease` | 通过；未使用官方签名 |
| Android `bundleStoreRelease` | 通过；未使用官方签名 |

Android 构建环境为 JDK 17、Android SDK 35、Gradle 8.9。构建中存在现有 Kotlin/Gradle 弃用提示及工具版本提示，未阻塞产物生成。

提交前另外检查 Git 跟踪文件中的常见密钥模式、私有配置凭据匹配、私人邮箱/机器路径、误入库产物和文档相对链接。检查脚本不能替代后续安全审计；发现问题请按 [安全反馈说明](../SECURITY.md) 处理。

## 验证边界

本轮未进行设备安装、模拟器 UI 操作或物理真机验证。Echo 联调不证明真实模型、小米账号、邮件、支付、系统推送及厂商通道已经在新实例可用；这些需要维护者配置自己的服务后单独验收。

未签名 Release 和 AAB 的构建成功不等于可以直接安装、已提交商店或已通过商店审核。GitHub Actions 的具体检查范围见 [工作流配置](../.github/workflows/ci.yml)，其结果不能替代设备与外部服务验收。
