# 参与 Auri

欢迎改进可靠性、文档、兼容性、测试及真实产品体验。提交前请阅读 [项目介绍](README.md)、[架构](docs/ARCHITECTURE.md) 和 [第三方声明](THIRD_PARTY_NOTICES.md)。

## 提交问题

说明版本/commit、发行渠道、Android 版本或 Python 环境、复现步骤、预期与实际结果。通知问题请分别说明消息是否落库、推送是否接受、手机是否展示、是否点击；不要把这些状态合并为“成功”。

请使用合成示例替代真实聊天、健康、位置、邮箱和支付信息。安全问题使用 [SECURITY.md](SECURITY.md) 的私下反馈途径。

## 开发与 PR

1. 从主分支创建独立分支，保持一个 PR 聚焦一个问题。
2. 修改 API 时同步 Android、schema、相关测试和文档。
3. 对行为修复加入能复现原问题的测试；避免只复述实现的测试。
4. 服务端运行 `python -m pytest -q`；Android 按改动运行单元测试和对应渠道构建。
5. UI、通知、定位、支付和升级请明确真机/模拟器/仅构建的验证范围。
6. PR 写清楚问题、最终行为、验证和未覆盖边界。

不要提交本机配置、数据库、APK、签名、供应商 Key、用户备份和运行日志。新增依赖注明用途与授权来源。

## 产品原则

保持连续聊天和非阻塞输入；只有生成时显示输入状态。所有关于现实世界的断言应来自可用、足够新鲜的证据。主动消息应尊重用户开关和免打扰状态。不要把产品规划中的能力当成已实现。

## 许可证

贡献者保留自己的版权。提交代码意味着你有权以目标组件当前许可证贡献该代码：服务端及根目录 AGPL-3.0-only，Android 原创代码 MIT，上游组件保留既有许可。未经授权不得粘贴私有代码或变更第三方许可。
